import StyledAvatarContainer from './styles/index'

const AvatarContainer = () => {
    return (
        <StyledAvatarContainer className="avatar-container">
            Avatar Creator
        </StyledAvatarContainer>
    );
}

export default AvatarContainer;